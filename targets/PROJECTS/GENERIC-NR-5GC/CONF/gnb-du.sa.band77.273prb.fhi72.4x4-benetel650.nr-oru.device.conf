RUs = (
{
  local_rf       = "no";
  nb_tx          = 4;
  nb_rx          = 4;
  att_tx         = 0;
  att_rx         = 0;
  bands          = [77];
  max_pdschReferenceSignalPower = -27;
  max_rxgain                    = 75;
  sf_extension                  = 0;
  eNB_instances  = [0];
  ru_thread_core = 12;
  sl_ahead       = 5;
  tr_preference  = "dma_if4p5_device"; # important: activate FHI7.2
                                # choose dma functions, act as slave(device)
  dma_ctrl_ip    = "192.168.100.1"; 
  dma_ctrl_port  = 3889;

  do_precoding   = 0; # needs to match O-RU configuration
}
);

log_config :
{
  global_log_level = "info";
  hw_log_level     = "info";
  phy_log_level    = "info";
  mac_log_level    = "info";
  rlc_log_level    = "info";
  pdcp_log_level   = "info";
  rrc_log_level    = "info";
  ngap_log_level   = "info";
  f1ap_log_level   = "info";
};

fhi_72 = {
  dpdk_devices = ("0000:03:00.0"); # one VF can be used as well
  system_core = 0;
  io_core = 10;
  worker_cores = (11);
  ru_addr = ("8c:1f:64:d1:10:46","8c:1f:64:d1:10:46");
  mtu = 9216;
  fh_config = ({
    T1a_cp_dl = (419, 470);
    T1a_cp_ul = (285, 336);
    T1a_up = (294, 345);
    Ta4 = (0, 200);
    ru_config = {
      iq_width = 9;
      iq_width_prach = 9;
    };
  });
};